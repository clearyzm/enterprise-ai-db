# Phase 9 简化实现清单

本文档列出 Phase 9 中所有简化的实现部分，便于后续 Phase 扩展和优化。

---

## 📋 简化部分总览

| 文件 | 简化项数量 | 优先级 |
|------|-----------|--------|
| `components/schema-form/index.tsx` | 5 项 | 🔴 高 |
| `lib/api.ts` | 4 项 | 🔴 高 |
| `app/(app)/datasets/[id]/new/page.tsx` | 3 项 | 🔴 高 |
| `app/(app)/datasets/[id]/[recordId]/edit/page.tsx` | 4 项 | 🔴 高 |
| `lib/permissions.ts` | 3 项 | 🟡 中 |
| `app/(app)/datasets/[id]/page.tsx` | 4 项 | 🟡 中 |
| `app/(app)/layout.tsx` | 5 项 | 🟢 低 |
| `app/(auth)/login/page.tsx` | 4 项 | 🟢 低 |

**总计**: 32 项简化

---

## 🔴 高优先级简化（必须在 Phase 10 前完成）

### 1. SchemaForm 组件 (5 项)
- ❌ 不支持 `array` 类型（动态列表）
- ❌ 不支持嵌套 `object` 类型
- ❌ 不支持 `$ref`（引用其他 dataset）
- ❌ 不支持日期选择器（`format: "date"`）
- ❌ 不支持文件上传（`format: "binary"`）

### 2. API 客户端 (4 项)
- ❌ 未实现请求队列（token 刷新期间暂停其他请求）
- ❌ 未实现请求取消（组件卸载时取消进行中的请求）
- ❌ 未实现自动重试（网络错误自动重试）
- ⚠️ **依赖警告**: 后端 `/auth/refresh` 端点未实现

### 3. 新增记录页 (3 项)
- ❌ 未集成 Zod 验证（无 zodResolver）
- ❌ 未实现表单自动保存（草稿）
- ❌ 未实现部门选择器（使用默认部门）

### 4. 编辑记录页 (4 项)
- ❌ 未集成 Zod 验证（无 zodResolver）
- ❌ 未实现表单自动保存（草稿）
- ❌ 未显示 diff 预览（提交前对比）
- ❌ 未实现乐观 UI 更新（显示待审批状态）

---

## 🟡 中优先级简化（Phase 10+ 可选）

### 5. 权限系统 (3 项)
- ❌ 未解析 `role.scope` 字段（硬编码映射）
- ❌ 未支持部门层级权限
- ❌ 未读取用户敏感度清除级别

### 6. 数据集详情页 (4 项)
- ❌ 未实现高级过滤（按字段值过滤）
- ❌ 未实现排序（点击表头排序）
- ❌ 未实现批量操作（多选 + 批量删除）
- ❌ 未实现导出功能（CSV/Excel）

---

## 🟢 低优先级简化（用户体验优化）

### 7. 应用布局 (5 项)
- ❌ 未实现侧边栏折叠/展开（移动端适配）
- ❌ 未实现用户菜单下拉（点击展开）
- ❌ 未实现通知中心（审批提醒）
- ❌ 未实现全局搜索（跨数据集搜索）
- ❌ 未实现 WebSocket provider（无实时更新）

### 8. 登录页 (4 项)
- ❌ 未实现"记住我"功能
- ❌ 未实现"忘记密码"流程
- ❌ 未实现验证码（防暴力破解）
- ❌ 未实现租户自动补全/选择器

---

## ✅ 实施建议

### Phase 10 必须完成
1. 集成 Zod 验证 (2-3 小时)
2. 扩展 SchemaForm 支持 array/object/$ref (4-6 小时)
3. 等待后端实现 Token 刷新 (后端任务)

### Phase 11 可选完成
4. 添加 shadcn/ui 组件 (3-4 小时)
5. 实现 WebSocket 实时更新 (4-6 小时)
6. 优化权限系统 (2-3 小时)

### Phase 12+ 体验优化
7. 移动端适配 (6-8 小时)
8. 高级表格功能 (8-10 小时)
9. 表单自动保存 (3-4 小时)

---

**所有简化部分已在源代码中用 `⚠️ SIMPLIFIED IMPLEMENTATION` 注释标明**
